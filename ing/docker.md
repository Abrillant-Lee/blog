#  Why？原理性，学新技术背景

- 物理机->虚拟机->容器

物理机诸多问题：部署非常慢、成本非常高、资源浪费、难于迁移和扩展
  - 业务版本不一样，对底层的依赖不一样，依赖是直接`yum install`安装的，底层依赖无法安装不同版本，“依赖能玩死你，一堆依赖”

虚拟机解决的物理机问题：环境隔离、应用迁移、省钱，但也有局限性，每个虚拟机都是完整的操作系统，要分配系统资源，

容器

