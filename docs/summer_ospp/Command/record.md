---
id: command-record
slug: /command-record
title: 记录不熟悉的命令/参数
authors: nan 
---

## **daemon-reload**
   - `daemon-reload` 是 `systemctl` 的一个选项，用于重新加载 systemd 管理器配置。它会重新扫描所有配置文件（包括新增的或修改的服务单元文件），但不会重启任何服务。使用这个选项的主要目的是在对服务单元文件进行更改后使这些更改生效。

